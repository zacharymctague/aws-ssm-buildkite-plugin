version: "2"
services:
  tests:
    image: buildkite/plugin-tester
    volumes:
      - ".:/plugin:ro"
  lint:
    image: buildkite/plugin-linter
    # command: ["--id", "a-github-user/file-counter"]
    command: ["--id", "blstrco/aws-ssm"]
    volumes:
      - ".:/plugin:ro"
